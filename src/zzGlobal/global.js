var Controller = SlackBot.Controller;

var registerBotCommand   = SlackBot.registerBotCommand;
var registerEvent        = SlackBot.registerEvent;
var registerSlashCommand = SlackBot.registerSlashCommand;

/* eslint no-unused-vars: ["error", { "varsIgnorePattern": "^.*$" }] */
